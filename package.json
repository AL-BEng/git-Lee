{
  "name": "git-learn",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint --ext .js,.vue src",
    "build:zip": "vue-cli-service build --modern && node scripts/zip-dist.js"
  },
  "dependencies": {
    "vue": "^3.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.0",
    "@vue/test-utils": "^2.4.6",
    "esbuild": "^0.18.0",
    "jsdom": "^22.0.0",
    "rollup": "^3.25.0",
    "vite": "^4.3.0",
    "vite-plugin-compression": "^0.5.1",
    "vitest": "^0.32.0"
  }
}
